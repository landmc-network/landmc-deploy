# Paper 26.2, behind the proxy.
#
# online-mode is false because the proxy authenticates and forwards the player's real profile.
# That is only safe because the backend is not reachable from outside the compose network -
# with modern forwarding on and the secret set, a direct connection is rejected anyway.
online-mode=false
server-ip=0.0.0.0
server-port=25565
motd=LandMC Lobby
max-players=500
view-distance=6
simulation-distance=4
spawn-protection=0
enable-command-block=false
allow-flight=true
enforce-secure-profile=false
level-name=lobby
level-type=minecraft\:flat
generate-structures=false
spawn-monsters=false
spawn-animals=false
spawn-npcs=false
difficulty=peaceful
gamemode=adventure
force-gamemode=true
pvp=false
enable-status=false
network-compression-threshold=-1
sync-chunk-writes=false
